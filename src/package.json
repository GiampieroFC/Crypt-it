{
  "name": "encryptor",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "build": "rimraf ./dist && tsc",
    "start": "pnpm build && node dist/main.js",
    "compile": "rimraf ./crypt-it.exe && pkg -t latest-win-x64 ./dist/main.js -o crypt-it.exe --build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/inquirer": "9.0.6",
    "@types/node": "20.8.9",
    "pkg": "5.8.1",
    "rimraf": "5.0.5"
  },
  "dependencies": {
    "colors": "1.4.0",
    "inquirer": "9.2.11"
  }
}